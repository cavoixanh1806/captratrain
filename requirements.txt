# ============================================================
# requirements.txt — CRNN+CTC CAPTCHA Solver
# ============================================================
# QUAN TRONG: PyTorch phai cai RIENG TRUOC (vi can chon CUDA hoac CPU)
#
# Buoc 1: Cai PyTorch (CHON 1 trong 2):
#
#   GPU NVIDIA (CUDA 12.8):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#   CPU only (khong co GPU):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Buoc 2: Cai cac package con lai:
#
#   pip install -r requirements.txt
# ============================================================

# ── Deep Learning ───────────────────────────────────────────
# (Cai rieng o Buoc 1, KHONG list o day vi pip se cai CPU version mac dinh)
# torch>=2.4.0
# torchvision>=0.19.0

# ── Xu ly anh ───────────────────────────────────────────────
opencv-python==4.9.0.80
Pillow==10.3.0

# ── Xu ly du lieu ───────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4

# ── Augmentation manh ───────────────────────────────────────
# Neu khong co: tu fallback ve torchvision (nhe hon nhung kem manh)
albumentations==1.4.3

# ── ONNX export (deploy cho ddddocr hoac onnxruntime) ───────
onnx>=1.15
onnxruntime>=1.17

# ── System info (cho system_info.py) ────────────────────────
psutil>=5.9.0

# ── Tien ich ────────────────────────────────────────────────
tqdm==4.66.2
